script=$(realpath "$0")
script_path=$(dirname "$script")
source ${script_path}/common.sh
component=cart
function_nodejs
# the next cmng all lines are same in all cmnds so we use the function cmnd to cold it.
